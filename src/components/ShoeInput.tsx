/*import { ShoesInputProps } from "../types";

function ShoesInput({ value, onChange, onRemove}: ShoesInputProps){
    return (
        <section className="shoe-item">
            <div className="prefix">EUROPA</div>
            <input type="number" value={value} onChange={(e) => onChange(Number(e.target.value))} className="shoe-input" />
            <button type="button" className="remove-btn" onClick={onRemove}>-</button>
        </section>
    )
}

export default ShoesInput;*/